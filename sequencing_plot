#!/usr/bin/python
# -*- coding: utf-8 -*-
import sys
from PyQt4 import QtGui, QtCore


class Graph(QtGui.QWidget):
    def __init__(self):
        super(Graph, self).__init__()

    def paintEvent(self, e):
        qp = QtGui.QPainter()
        qp.begin(self)
        self.draw_lines(qp)
        qp.end()

    def draw_lines(self, qp):

        pen = QtGui.QPen(QtCore.Qt.black, 2, QtCore.Qt.SolidLine)
        qp.setPen(pen)
        qp.drawLine(80, 40, 80, 210)  # y axis
        self.drawArrowHead(qp, 75, 40, 85, 40, 80, 35)
        qp.drawLine(80, 210, 600, 210)  # x axis
        self.drawArrowHead(qp, 600, 205, 600, 215, 605, 210)

        pen = QtGui.QPen(QtCore.Qt.darkGray, 1, QtCore.Qt.SolidLine)  # horizontal dashed
        pen.setStyle(QtCore.Qt.DashLine)
        qp.setPen(pen)
        qp.drawLine(80, 60, 600, 60)
        qp.drawLine(80, 190, 600, 190)

        pen = QtGui.QPen(QtCore.Qt.darkGray, 1, QtCore.Qt.SolidLine)  # vertical dashed
        pen.setStyle(QtCore.Qt.DashLine)
        qp.setPen(pen)
        qp.drawLine(150, 60, 150, 210)
        qp.drawLine(220, 60, 220, 210)
        qp.drawLine(290, 60, 290, 210)
        qp.drawLine(360, 60, 360, 210)
        qp.drawLine(430, 60, 430, 210)
        qp.drawLine(500, 60, 500, 210)
        qp.drawLine(570, 60, 570, 210)

        pen = QtGui.QPen(QtCore.Qt.blue, 2, QtCore.Qt.SolidLine)  # draw graph
        qp.setPen(pen)
        qp.drawLine(80, 209, 150, 209)
        qp.drawLine(150, 209, 220, 60)
        qp.drawLine(430, 60, 500, 209)  # when cut off is 0
        qp.drawLine(500, 209, 570, 209)  # when cut off is 0
        pen.setStyle(QtCore.Qt.CustomDashLine)
        pen.setDashPattern([35, 13, 10, 13])
        qp.setPen(pen)
        qp.drawLine(220, 60, 430, 60)

        pen = QtGui.QPen(QtCore.Qt.black, 1, QtCore.Qt.SolidLine)  # draw bottom legend lines
        qp.setPen(pen)
        qp.drawLine(80, 220, 80, 250)
        qp.drawLine(150, 220, 150, 250)
        qp.drawLine(220, 220, 220, 240)
        qp.drawLine(290, 220, 290, 250)
        qp.drawLine(360, 220, 360, 250)
        qp.drawLine(430, 220, 430, 250)
        qp.drawLine(500, 220, 500, 240)
        qp.drawLine(570, 220, 570, 250)

        # draw arrows
        qp.drawLine(90, 225, 140, 225)
        self.draw_arrow_head(qp, 90, 220, 90, 230, 85, 225)
        self.draw_arrow_head(qp, 140, 220, 140, 230, 145, 225)
        qp.drawLine(160, 225, 210, 225)
        self.draw_arrow_head(qp, 160, 220, 160, 230, 155, 225)
        self.draw_arrow_head(qp, 210, 220, 210, 230, 215, 225)
        qp.drawLine(160, 250, 280, 250)
        self.draw_arrow_head(qp, 160, 245, 160, 255, 155, 250)
        self.draw_arrow_head(qp, 280, 245, 280, 255, 285, 250)
        qp.drawLine(370, 225, 420, 225)
        self.draw_arrow_head(qp, 370, 220, 370, 230, 365, 225)
        self.draw_arrow_head(qp, 420, 220, 420, 230, 425, 225)
        qp.drawLine(440, 225, 490, 225)
        self.draw_arrow_head(qp, 440, 220, 440, 230, 435, 225)
        self.draw_arrow_head(qp, 490, 220, 490, 230, 495, 225)
        qp.drawLine(440, 250, 560, 250)
        self.draw_arrow_head(qp, 440, 245, 440, 255, 435, 250)
        self.draw_arrow_head(qp, 560, 245, 560, 255, 565, 250)

    def draw_arrow_head(self, qp, a, b, c, d, e, f):
        # draw arrow head
        qp.setBrush(QtGui.QBrush(QtCore.Qt.black))
        points = [QtCore.QPoint(a, b), QtCore.QPoint(c, d), QtCore.QPoint(e, f)]
        needle = QtGui.QPolygon(points)
        qp.drawPolygon(needle)

